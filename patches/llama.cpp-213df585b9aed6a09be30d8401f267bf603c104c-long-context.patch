diff --git a/ggml/src/ggml-metal/ggml-metal-device.m b/ggml/src/ggml-metal/ggml-metal-device.m
index 19c5782..87b13bf 100644
--- a/ggml/src/ggml-metal/ggml-metal-device.m
+++ b/ggml/src/ggml-metal/ggml-metal-device.m
@@ -1177,2 +1177,6 @@ ggml_metal_device_t ggml_metal_device_init(int device, int n_devices) {
             dev->props.use_residency_sets = getenv("GGML_METAL_NO_RESIDENCY") == nil;
+            if (!dev->props.use_residency_sets) {
+                fprintf(stderr, "%s: use residency sets    = false (GGML_METAL_NO_RESIDENCY=1)\n", __func__);
+                fflush(stderr);
+            }
 #endif
diff --git a/src/llama-kv-cache.cpp b/src/llama-kv-cache.cpp
index ec0f5a7..86f7691 100644
--- a/src/llama-kv-cache.cpp
+++ b/src/llama-kv-cache.cpp
@@ -79,3 +79,4 @@ llama_kv_cache::llama_kv_cache(
     const  layer_reuse_cb & reuse,
-    const  layer_share_cb & share) :
+    const  layer_share_cb & share,
+                     bool   key_only) :
     model(model), hparams(hparams), v_trans(v_trans),
@@ -228,3 +229,3 @@ llama_kv_cache::llama_kv_cache(
         const bool has_k = true;
-        const bool has_v = !is_mla;
+        const bool has_v = !is_mla && !key_only;
 
diff --git a/src/llama-kv-cache.h b/src/llama-kv-cache.h
index 6cb6dbd..be76592 100644
--- a/src/llama-kv-cache.h
+++ b/src/llama-kv-cache.h
@@ -114,3 +114,4 @@ public:
         const  layer_reuse_cb & reuse,
-        const  layer_share_cb & share);
+        const  layer_share_cb & share,
+                         bool   key_only = false);
 
diff --git a/src/llama-memory-hybrid-idx.cpp b/src/llama-memory-hybrid-idx.cpp
index 35059ba..bba8bd6 100644
--- a/src/llama-memory-hybrid-idx.cpp
+++ b/src/llama-memory-hybrid-idx.cpp
@@ -22,2 +22,5 @@ llama_memory_hybrid_idx::llama_memory_hybrid_idx(
                 ggml_type   type_v,
+                            /* indexer */
+                ggml_type   type_k_idx,
+                ggml_type   type_v_idx,
                      bool   v_trans,
@@ -53,7 +56,9 @@ llama_memory_hybrid_idx::llama_memory_hybrid_idx(
         LLAMA_LOG_INFO("%s: creating indexer KV cache, size = %u cells\n", __func__, kv_size);
+        LLAMA_LOG_INFO("%s: Qwen4Exp indexer cache types: K = %s, V = none (key-only)\n",
+                __func__, ggml_type_name(type_k_idx));
 
         return new llama_kv_cache(
-            model, hparams_idx, type_k, type_v, v_trans, offload, unified,
+            model, hparams_idx, type_k_idx, type_v_idx, v_trans, offload, unified,
             kv_size, n_seq_max, n_pad, n_swa, swa_type,
-            nullptr, filter_idx, nullptr, nullptr);
+            nullptr, filter_idx, nullptr, nullptr, true);
     }()) {}
diff --git a/src/llama-memory-hybrid-idx.h b/src/llama-memory-hybrid-idx.h
index a17e184..55b861a 100644
--- a/src/llama-memory-hybrid-idx.h
+++ b/src/llama-memory-hybrid-idx.h
@@ -24,2 +24,5 @@ public:
                 ggml_type   type_v,
+                            /* indexer */
+                ggml_type   type_k_idx,
+                ggml_type   type_v_idx,
                      bool   v_trans,
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index 8b24ed5..cc81483 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -31,2 +31,3 @@
 #include <cstdint>
+#include <cstdlib>
 #include <cstring>
@@ -1667,3 +1668,8 @@ bool llama_model_base::load_tensors(llama_model_loader & ml) {
 
-    ml.init_mappings(true, use_mlock ? &pimpl->mlock_mmaps : nullptr);
+    const char * mmap_prefetch_env = std::getenv("LLAMA_MMAP_PREFETCH");
+    const bool mmap_prefetch = mmap_prefetch_env == nullptr || std::strcmp(mmap_prefetch_env, "0") != 0;
+    if (!mmap_prefetch) {
+        LLAMA_LOG_INFO("%s: mmap prefetch disabled by LLAMA_MMAP_PREFETCH=0\n", __func__);
+    }
+    ml.init_mappings(mmap_prefetch, use_mlock ? &pimpl->mlock_mmaps : nullptr);
     pimpl->mappings.reserve(ml.mappings.size());
@@ -2489,3 +2495,4 @@ llama_memory_i * llama_model::create_memory(const llama_memory_params & params,
                     } else if (needs_mem_idx) {
-                        // sparse attention over a per-token indexer cache, in its own memory type
+                        // Sparse attention uses an F16 indexer cache so routing scores are not
+                        // perturbed by the quantized main attention cache.
                         res = new llama_memory_hybrid_idx(
@@ -2494,2 +2501,4 @@ llama_memory_i * llama_model::create_memory(const llama_memory_params & params,
                             /* attn_type_v       */ params.type_v,
+                            /* indexer_type_k    */ GGML_TYPE_F16,
+                            /* indexer_type_v    */ GGML_TYPE_F16,
                             /* attn_v_trans      */ !cparams.flash_attn,
diff --git a/src/models/models.h b/src/models/models.h
index b22367f..9f25b0f 100644
--- a/src/models/models.h
+++ b/src/models/models.h
@@ -2328,2 +2328,7 @@ struct llama_model_qwen4exp : public llama_model_base {
 
+        // Owned by llm_graph_result; optionally shared by all QSA layers because cache
+        // layout and visibility metadata are layer-independent.
+        llm_graph_input_i * qsa_input = nullptr;
+        const bool qsa_share_inputs;
+
         ggml_tensor * build_layer_attn_linear(
diff --git a/src/models/qwen4exp.cpp b/src/models/qwen4exp.cpp
index c438710..3c5de20 100644
--- a/src/models/qwen4exp.cpp
+++ b/src/models/qwen4exp.cpp
@@ -6,2 +6,14 @@
 #include <algorithm>
+#include <cstdlib>
+#include <cstring>
+
+static bool qwen4exp_share_qsa_inputs() {
+    const char * value = std::getenv("LLAMA_QSA_SHARED_INPUTS");
+    if (value == nullptr || std::strcmp(value, "0") == 0) {
+        return false;
+    }
+
+    GGML_ASSERT(std::strcmp(value, "1") == 0 && "LLAMA_QSA_SHARED_INPUTS must be 0 or 1");
+    return true;
+}
 
@@ -257,5 +269,9 @@ ggml_tensor * llama_model_qwen4exp::graph::build_hc_combine(
 llama_model_qwen4exp::graph::graph(const llama_model & model, const llm_graph_params & params) :
-    llm_build_delta_net_base(params), model(model) {
+    llm_build_delta_net_base(params), qsa_share_inputs(qwen4exp_share_qsa_inputs()), model(model) {
     const int64_t hc = hparams.dsv4_hc_mult;
 
+    if (qsa_share_inputs) {
+        LLAMA_LOG_INFO("%s: Qwen4Exp QSA graph inputs shared across layers (LLAMA_QSA_SHARED_INPUTS=1)\n", __func__);
+    }
+
     GGML_ASSERT(hparams.n_embd_head_v() == hparams.n_embd_head_k());
@@ -423,17 +439,36 @@ ggml_tensor * llama_model_qwen4exp::graph::build_qsa_top_k(
 
-    auto qsa = std::make_unique<llm_graph_input_qsa>(mctx_hyb, (uint32_t) r);
+    llm_graph_input_qsa * inp;
+
+    if (!qsa_share_inputs || qsa_input == nullptr) {
+        auto qsa = std::make_unique<llm_graph_input_qsa>(mctx_hyb, (uint32_t) r);
 
-    qsa->k_idxs    = mctx_idx->build_input_k_idxs(ctx0, ubatch);
-    qsa->cell_blk  = ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, n_kv, n_stream);
-    qsa->blk_cells = ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, r*n_blocks, n_stream);
-    qsa->blk_pos   = ggml_new_tensor_1d(ctx0, GGML_TYPE_I32, 4*n_blocks*n_stream);
-    qsa->bias      = ggml_new_tensor_3d(ctx0, GGML_TYPE_F32, n_kv, n_tps, n_stream);
+        qsa->k_idxs    = mctx_idx->build_input_k_idxs(ctx0, ubatch);
+        qsa->cell_blk  = ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, n_kv, n_stream);
+        qsa->blk_cells = ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, r*n_blocks, n_stream);
+        qsa->blk_pos   = ggml_new_tensor_1d(ctx0, GGML_TYPE_I32, 4*n_blocks*n_stream);
+        qsa->bias      = ggml_new_tensor_3d(ctx0, GGML_TYPE_F32, n_kv, n_tps, n_stream);
 
-    ggml_set_input(qsa->cell_blk);
-    ggml_set_input(qsa->blk_cells);
-    ggml_set_input(qsa->blk_pos);
-    ggml_set_input(qsa->bias);
+        ggml_set_input(qsa->cell_blk);
+        ggml_set_input(qsa->blk_cells);
+        ggml_set_input(qsa->blk_pos);
+        ggml_set_input(qsa->bias);
 
-    llm_graph_input_qsa * inp = qsa.get();
-    res->add_input(std::move(qsa));
+        inp = qsa.get();
+        llm_graph_input_i * added = res->add_input(std::move(qsa));
+        if (qsa_share_inputs) {
+            qsa_input = added;
+        }
+    } else {
+        // Cache layout, visibility, and tail membership are common to every QSA layer.
+        // Reuse their byte-identical graph inputs instead of allocating and filling twelve
+        // F32 [n_kv, n_tps, n_stream] bias tensors. Fail closed if a future architecture
+        // introduces per-layer compression or a different context.
+        inp = static_cast<llm_graph_input_qsa *>(qsa_input);
+        GGML_ASSERT(inp->mctx == mctx_hyb);
+        GGML_ASSERT(inp->ratio == (uint32_t) r);
+        GGML_ASSERT(inp->cell_blk->ne[0] == n_kv && inp->cell_blk->ne[1] == n_stream);
+        GGML_ASSERT(inp->blk_cells->ne[0] == r*n_blocks && inp->blk_cells->ne[1] == n_stream);
+        GGML_ASSERT(inp->blk_pos->ne[0] == 4*n_blocks*n_stream);
+        GGML_ASSERT(inp->bias->ne[0] == n_kv && inp->bias->ne[1] == n_tps && inp->bias->ne[2] == n_stream);
+    }
 
